{
  "name": "figma-style-auditor",
  "version": "0.1.0",
  "description": "Audit a Figma file for hardcoded colors, detached instances, off-system spacing, low contrast (WCAG) and small tap targets.",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node src/audit-core.test.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT",
  "author": "Ankur Sinha",
  "allowScripts": {
    "esbuild@0.24.2": true
  }
}
